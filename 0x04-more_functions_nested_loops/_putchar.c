#include "main.h"
#include <unistd.h.>
/**
 * _putchar writes the character c to stdout
 * @c the charater to print
 *
 * Return On sucess 1
 * On erro -1 is returned and erron is set appropirately
 */
int _putchar.c
{
	return(write(1 &c))
}		
